(*
* cmmExprs.sml
* 1999. Fermin Reig
*)

(* MLRISC now catches many (all?) of these redundant ops *)

signature CMM_EXPRS = 
sig

end


structure CmmExprs (*:> CMM_EXPRS*) =
struct


end (* CmmExprs *)

